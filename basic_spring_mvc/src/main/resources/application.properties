# Enable Actuator shutdown endpoint and expose all endpoints via web interface
management.endpoint.shutdown.enabled=true
management.endpoints.web.exposure.include=*

# Serve static resources out of source directory (for faster development, but disable for a production build)
spring.web.resources.static-locations=file:src/main/resources/static
